# Deny all ingress and egress by default
# All pods must explicitly allow traffic
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: supabase
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
